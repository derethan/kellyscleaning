const Divider = () => {
    return (
        <div style={{
            position: 'relative',
            height: '1px',
            background: 'linear-gradient(to right, transparent, var(--color-primary), transparent)',
            marginBottom: '1.5rem',
        }} >
            
        </div>
    )
}

export default Divider